package Requests;

import Enums.Day;
import Enums.RequestStatus;
import Enums.RequestType;
import Enums.Shift;
import Models.Employee;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import static org.junit.Assert.*;

public class RequestTest {
    /*
    Request request;
    RequestDay rd;
    Employee emp;

    @BeforeEach
    void init() {
        request = new Request();
        emp = new Employee();
        emp.setName("Bailey");
        emp.setId(1);
        emp.setManager(false);
        rd = new RequestDay();
        rd.setDay(Day.MONDAY);
        rd.setShift(Shift.Day);
    }

    @Test
    void testApprove() {
        request.approve();
        assertEquals(RequestStatus.APPROVED, request.getStatus());
    }

    @Test
    void testDeny() {
        request.deny();
        assertEquals(RequestStatus.DENIED, request.getStatus());
    }

    @Test
    void testViewStatus() {
        assertEquals(RequestStatus.PENDING, request.viewStatus());
    }

    @Test
    void testGetEmp() {
        emp.setName("Bailey");
        assertEquals("Bailey", request.getEmp().getName());
    }

    @Test
    void testGetDay() {
        request.setDay(rd);
        assertEquals(Day.MONDAY, request.getDay().getDay());
    }

    @Test
    void testGetId() {
        request.setId(1);
        assertEquals(1, request.getId());
    }

    @Test
    void testGetReason() {
        request.setReason("Because");
        assertEquals("Because", request.getReason());
    }

    //TODO: COMMIT TESTS!


     */
}
